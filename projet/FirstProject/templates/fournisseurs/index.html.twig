{% extends 'base.html.twig' %}

{% block title %}Fournisseurs index{% endblock %}

{% block body %}
    <h1>Fournisseurs index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
               
               
                <th>Tel</th>
                <th>Adresse</th>
                <th>Nom entreprise</th>
            
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fournisseur in fournisseurs %}
            <tr>
                <td>{{ fournisseur.id }}</td>
                <td>{{ fournisseur.prenom }} {{ fournisseur.nom }}</td>
               
               
                <td>{{ fournisseur.tel }}</td>
                <td>{{ fournisseur.adresse }}</td>
                <td>{{ fournisseur.nomEntreprise }}</td>
           
                <td>
                    <a href="{{ path('fournisseurs_show', {'id': fournisseur.id}) }}">show</a>
                    <a href="{{ path('fournisseurs_edit', {'id': fournisseur.id}) }}">edit</a>
                     {{ include('fournisseurs/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('fournisseurs_new') }}">Create new</a>
{% endblock %}
